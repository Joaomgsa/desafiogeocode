import React from "react";
import { Container } from "react-bootstrap";

export default function Sorteio(){
    return(
        <div>
            <Container>
                <h2>Exibir grupos da copa do Mundo</h2>
            </Container>
        </div>
    );
}